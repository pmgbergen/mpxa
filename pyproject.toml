[build-system]
requires = ["setuptools", "wheel", "pybind11>=2.6.0"]
build-backend = "setuptools.build_meta"


[project]
name = "mpxa"
version = "0.0.1"
dependencies = [
    "numpy>=2.2.4",
    "pybind11>=2.13.6",
]
